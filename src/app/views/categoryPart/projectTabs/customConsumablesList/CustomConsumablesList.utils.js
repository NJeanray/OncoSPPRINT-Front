import React from 'react'
import { FormattedMessage } from 'react-intl'

export const customConsumableCreatedText = (
  <FormattedMessage id="customConsumableList.createCustomConsumable" />
)
export const customConsumableUpdatedText = (
  <FormattedMessage id="customConsumableList.updateCustomConsumable" />
)
export const customConsumableDeletedText = (
  <FormattedMessage id="customConsumablesList.deleteCustomConsumable" />
)
