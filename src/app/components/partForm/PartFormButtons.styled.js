import styled from 'styled-components'
import Box from '@material-ui/core/Box'

const PartFormButtonsWrapper = styled(Box)`
  .MuiGrid-container {
    & > div {
      margin-top: 20px;
    }
  }
`

export default PartFormButtonsWrapper
