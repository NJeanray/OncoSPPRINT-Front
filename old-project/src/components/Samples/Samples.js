import React from 'react'

import BrickFrequency from '../Brick/BrickFrequency'

const onChange = e => {}

const Samples = props => <BrickFrequency brick={null} bricks={null} onChange={onChange} />

export default Samples
